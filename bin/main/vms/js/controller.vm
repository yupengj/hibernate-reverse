#set($beanPrefix="gantang."+${bean.beanPackage}+"."+${bean.beanName})
Ext.define('${beanPrefix}Controller', {
	extend : "Ext.app.Controller",
	models : ['${beanPrefix}Model'],
	views : ['${beanPrefix}View'],
	init : function(application) {
		this.control({
		});
	},
	mainView : '${beanPrefix}View',
	viewConfig : {
		width : '95%',
		height : '90%'
	}
})